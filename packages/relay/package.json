{
  "name": "@taskyon/relay",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/relay.mjs",
  "scripts": {
    "start": "yarn build && node dist/relay.mjs",
    "build": "vite build --config vite.config.relay.ts"
  },
  "dependencies": {
    "@taskyon/p2p-core": "workspace:*"
  },
  "devDependencies": {
    "vite": "^7.0.3"
  }
}
